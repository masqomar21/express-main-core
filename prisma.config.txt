import 'dotenv/config'
import { defineConfig, env } from 'prisma/config'

const databaseUrl = `postgresql://${env('DB_USERNAME')}:${env('DB_PASSWORD')}@${env('DB_HOST')}:5432/${env('DB_NAME')}?timezone=${env('DB_TIMEZONE')}`

export default defineConfig({
  schema: 'prisma/schema.prisma',
  migrations: {
    path: 'prisma/migrations',
  },
  datasource: {
    url: databaseUrl,
  },

})